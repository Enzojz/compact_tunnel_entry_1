data = function()
    return {
        availability = {
            yearFrom = 0,
            yearTo = 0,
        },
        buildMode = "SINGLE",
        cost = {
            price = 0,
        },
        description = {
            name = _("FREENODE_MARKER"),
            description = _("DESC_FREENODE_MARKER"),
            icon = "ui/ste/freenode.tga"
        },
        category = {
            categories = {_("STRUCTURE")},
        },
        type = "ste_freenode",
        order = {
            value = 99,
        },
        metadata = {
            isTrack = false,
            type = "ste_freenode"
        },
        
        updateFn = function(result, transform, tag, slotId, addModelFn, params) end,
        
        getModelsFn = function(params) return {} end
    }

end
