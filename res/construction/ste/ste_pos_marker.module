data = function()
    return {
        availability = {
            yearFrom = 0,
            yearTo = 0,
        },
        buildMode = "SINGLE",
        cost = {
            price = 0,
        },
        description = {
            name = _("NAME_MARKER"),
            description = _("DESC_MARKER"),
            icon = "ui/ste/adjuster.tga"
        },
        category = {
            categories = {_("STRUCTURE")},
        },
        type = "ste_marker",
        order = {
            value = 99,
        },
        metadata = {
            isTrack = false,
            type = "ste_marker"
        },
        
        updateFn = function(result, transform, tag, slotId, addModelFn, params) end,
        
        getModelsFn = function(params) return {} end
    }

end
